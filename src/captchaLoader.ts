import {DotNet} from  "@microsoft/dotnet-js-interop";

class captchaLoader {
    async loadAsync(siteKey: string, dotNetObjRef: DotNet.DotNetObject): Promise<void> {

    }

    async executeAsync(siteKey: string, dotNetObjRef: DotNet.DotNetObject): Promise<void> {

    }

    async renderAsync(siteKey: string, dotNetObjRef: DotNet.DotNetObject): Promise<void> {
        
    }
}